package service;

import entity.Address;
import entity.Mistakable;
import entity.Name;
import entity.Phone;

import java.util.Collection;
import java.util.Collections;

public class MistakeMaker {

    public Collection<Mistakable> makeMistake(Collection collection, int coefficient) {

//        Collection<Mistakable>collections =  collection.stream()
//                .limit(collection.size()*coefficient)
//                .forEach((e)-> (e.makeMistake());
//
        return null;
    }
}
